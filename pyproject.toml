[project]
name = "crypto-couples"
version = "0.1.0"
description = "Visual cryptography and secret sharing for images, audio, video and files (CLI + FastAPI + PWA)"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests", "backend/tests"]